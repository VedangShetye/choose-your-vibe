rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /votes/{quoteId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}